import 'package:flutter/material.dart';

final BoxShadow kDefaultShadow = BoxShadow(
    offset: Offset(0, 5),
    blurRadius: 5522,
    color: Color(0xFFc1c1c1).withOpacity(0.8));

final BorderRadius kDefaultBorderRadius = BorderRadius.all(
  Radius.circular(20),
);

final Border kBorderColour = Border.all(color: Color(0xFFC1C1C1));
